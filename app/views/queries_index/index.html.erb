<head>
  <title>Queued queries</title>
  <link rel="stylesheet" href="https://unpkg.com/chota@latest">

<body>
  <h1>Queries</h1>

  <table class="striped">
    <thead>
      <tr>
        <th>Query id</th>
        <th>Statement</th>
        <th>Read timeout</th>
        <th>SPAQL limit</th>
        <th>Answer limit</th>
        <th>State</th>
        <th>Queued at</th>
        <th>Started at</th>
        <th>Finished at</th>
        <th>Elapsed Time(s)</th>
        <th>Number of answers</th>
      </tr>
    </thead>

    <tbody>
      <% @queries.each do |query| %>
        <tr>
          <td><%= query.query_id %></td>
          <td><%= query.statement %></td>
          <td><%= query.read_timeout %></td>
          <td><%= query.sparql_limit %></td>
          <td><%= query.answer_limit %></td>
          <td><%= query.state %></td>
          <td><%= query.queued_at.in_time_zone.strftime('%H:%M') %></td>
          <td><%= query.started_at&.in_time_zone&.strftime('%H:%M') %></td>
          <td><%= query.finished_at&.in_time_zone&.strftime('%H:%M') %></td>
          <td><%= number_with_precision(query.elapsed_time, precision: 1) %></td>
          <td><%= query.answers.size %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
